//Styling the background 
//index.tsx................................
import { Text, View, StyleSheet, SafeAreaView, Platform, StatusBar} from "react-native";
// import Constants from 'expo-constants';

import { colors } from '../src/utils/colors';


export default function Index() {
  return (
    <SafeAreaView style = {styles.container}>
      <Text style = {styles.text}>Hello World!</Text>
      <Text style = {styles.text}>Sai Saw Mong Marma!</Text>
    </SafeAreaView>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    paddingTop: Platform.OS === "android" ? StatusBar.currentHeight : 0,
    backgroundColor: colors.darkBlue
  },

  text: {
    color: colors.white
  }
});

//colors.js file is in ../src/utils folder
//colors.js................................
export const colors = {
    white: "#fff",
    darkBlue: "#252250"
}

//temporary we are commenting _layout.tsx file
//because it shows index file in navebar

//